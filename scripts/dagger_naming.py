#!/usr/bin/env python
"""CLI for pcdagger.dagger.naming (the orchestrator's name derivations); the module's __main__ does the work."""

import runpy

runpy.run_module("pcdagger.dagger.naming", run_name="__main__")
